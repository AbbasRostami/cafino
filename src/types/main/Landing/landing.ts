export interface CategorySection {
  id: string;
  name: string;
  image: string;
  itemCount: number;
}

export interface ItemSection {
  id: string;
  name: string;
  description: string;
  price: number;
  image: string;
  rating: number;
}
